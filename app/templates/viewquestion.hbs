<div class="singleview">

  <img src="{{post_info.image_url}}">
 <p>Taken by: {{creator.user_name}}</p>

{{!--   {{#if visited}}
    <p>This has been visited</p>
  {{else}}
    <p>This has NOT been visited</p>
  {{/if}} --}}
  <input type="text" id="guess" placeholder="What's your guess?">
  <button type="submit" name="action">Submit</button>
  <button id="hint" data-id="{{id}}">Hint</button>

</div>
